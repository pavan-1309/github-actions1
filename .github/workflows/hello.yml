name: hello-world
on:
  push:
    branches: [ main ]
jobs:
    say-hello:
        runs-on: ubuntu-latest
        steps:
            - name: Hello world action step
              run: echo "Hello, world!"